services:
  web:
    build:
      context: ./demo-web/harmonic-demo
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
